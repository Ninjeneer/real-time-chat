export default function removePassword(req, res): void {
    // Check in every attribute of res if there is a user with a password, and delete it
    // for (const key in Object.keys(res.) {
    //         if (typeof res[key] === 'object') {
    //             removePassword(req, res[key]);
    //         } else if (key === 'password') {
    //             delete res[key];
    //         }
    // }
}